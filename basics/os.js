const os = require("os");
// You can import at the bottom also
// but it will not look good
// import at the top

console.log(os.platform());

console.log(os.hostname());
console.log(os.freemem());
console.log(os.homedir());
